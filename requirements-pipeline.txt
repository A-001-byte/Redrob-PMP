# Glasshouse — rank-time dependencies only (used by the Dockerfile).
# Same pins as requirements.txt minus the Phase-4 web stack
# (fastapi, uvicorn, streamlit), which the ranker never imports.
sentence-transformers==5.5.1
faiss-cpu==1.14.2
rank-bm25==0.2.2
numpy==2.4.3
scikit-learn==1.8.0
pandas==3.0.2
python-docx==1.2.0
python-dateutil==2.9.0.post0
tqdm==4.67.3
pyyaml==6.0.3
huggingface-hub==1.18.0
