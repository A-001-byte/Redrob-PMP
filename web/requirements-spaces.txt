# HuggingFace Spaces requirements (copy to the Space repo root as
# requirements.txt). Streamlit UI + everything pipeline\rank.py needs for
# the in-app Re-rank button (CPU-only, models loaded from models\).
streamlit>=1.45
pandas>=2.0
numpy>=1.26
faiss-cpu>=1.8
rank_bm25>=0.2.2
sentence-transformers>=2.7
tqdm>=4.66
